在做出任何建模决策之前，先分析医学成像数据集（采集网格、体素间距和方向扩展、强度域、实际存在的标签值、目标占据的体积大小以及目标的毫升大小），然后根据研究人员声明的计划对该配置文件进行门控。在它仍然便宜的情况下，捕获训练运行后否则会出现的数据集事实：不包含基本事实的“测试集”，网格与图像不匹配的标签，杂散标签索引，目标只占百分之几，而准确性被计划为指标，以及没有人宣布重采样决策的采集异质性。发出数据集配置文件 JSON 和读取它的确定性门（仅限 stdlib，因此审计随 JSON 一起进行）。它描述数据并根据数据审核计划；它不进行预处理、分割或训练。
