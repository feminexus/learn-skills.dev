База знаний по разработке Odoo 19 с 18 специализированными руководствами, охватывающими действия (ir.actions.*, задания cron, действия сервера), контроллеры (HTTP-маршрутизация, конечные точки, типы аутентификации), файлы данных (записи XML/CSV, ярлыки, noupdate), декораторы API (@api.dependents, @api.constrains, @api.ondelete, @api.onchange, @api.model, @api.private), SQL Ограничения (models.Constraint заменяет _sql_constraints), индексы базы данных (models.Index), разработка модулей (манифест, мастера, отчеты), типы полей (Char, Text, Monetary, реляционные поля), конфигурация манифеста (__manifest__.py, зависимости, пакеты ресурсов), миксины (mail.thread, mail.activity.mixin, mail.alias.mixin, utm.mixin), методы модели ORM (поиск, CRUD, фильтры домена, наборы записей, именование моделей CamelCase), сценарии миграции (перехватчики до, после и конца, миграция данных), компоненты внешнего интерфейса OWL (перехватчики, службы, жизненный цикл), оптимизация производительности (предотвращение N+1, пакетные операции, _read_group), отчеты QWeb (PDF/HTML, бумажные форматы, штрих-коды, t-out), безопасность/ACL (правила записи, разрешения полей, группы на основе привилегий, @api.private), Тестирование (TransactionCase, HttpCase, макетирование, утверждения количества запросов), Транзакции (точки сохранения, UniqueViolation, ошибки сериализации), Переводы (i18n, PO-файлы, переводимые поля), XML-представления (список/форма/поиск, шаблоны карточек канбан, наследование xpath, шаблоны QWeb). Используйте при написании, проверке или отладке любого кода Odoo 19 Python или XML, создании или изменении модулей, устранении проблем с производительностью или поиске шаблонов API Odoo 19 и лучших практик.
