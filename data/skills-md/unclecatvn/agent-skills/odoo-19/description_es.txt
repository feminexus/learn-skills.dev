Base de conocimientos de desarrollo de Odoo 19 con 18 guías especializadas que cubren acciones (ir.actions.*, trabajos cron, acciones de servidor), controladores (enrutamiento HTTP, puntos finales, tipos de autenticación), archivos de datos (registros XML/CSV, accesos directos, noupdate), decoradores de API (@api.depends, @api.constrains, @api.ondelete, @api.onchange, @api.model, @api.private), SQL Restricciones (models.Constraint que reemplaza _sql_constraints), índices de bases de datos (models.Index), desarrollo de módulos (manifiesto, asistentes, informes), tipos de campos (característicos, texto, monetarios, campos relacionales), configuración de manifiesto (__manifest__.py, dependencias, paquetes de activos), Mixins (mail.thread, mail.activity.mixin, mail.alias.mixin, utm.mixin), métodos de modelo ORM (búsqueda, CRUD, filtros de dominio, conjuntos de registros, denominación de modelos CamelCase), scripts de migración (hooks previos, posteriores y finales, migración de datos), componentes frontend OWL (hooks, servicios, ciclo de vida), optimización del rendimiento (prevención N+1, operaciones por lotes, _read_group), informes QWeb (PDF/HTML, formatos de papel, códigos de barras, t-out), seguridad/ACL (reglas de registro, permisos de campo, grupos basados en privilegios, @api.private), pruebas (TransactionCase, HttpCase, burlas, aserciones de recuento de consultas), Transacciones (puntos de guardado, UniqueViolation, fallas de serialización), Traducciones (i18n, archivos PO, campos traducibles), Vistas XML (lista/formulario/búsqueda, plantillas de tarjetas kanban, herencia xpath, plantillas QWeb). Úselo al escribir, revisar o depurar cualquier código Python o XML de Odoo 19, crear o modificar módulos, solucionar problemas de rendimiento o buscar patrones y mejores prácticas de API de Odoo 19.
