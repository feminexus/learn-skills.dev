Base de connaissances de développement Odoo 19 avec 18 guides spécialisés couvrant les actions (ir.actions.*, tâches cron, actions serveur), les contrôleurs (routage HTTP, points de terminaison, types d'authentification), les fichiers de données (enregistrements XML/CSV, raccourcis, noupdate), les décorateurs d'API (@api.depends, @api.constrains, @api.ondelete, @api.onchange, @api.model, @api.private), les contraintes SQL. (models.Constraint remplaçant _sql_constraints), index de base de données (models.Index), développement de modules (manifeste, assistants, rapports), types de champs (char, texte, monétaire, champs relationnels), configuration du manifeste (__manifest__.py, dépendances, bundles d'actifs), mixins (mail.thread, mail.activity.mixin, mail.alias.mixin, utm.mixin), méthodes du modèle ORM (recherche, CRUD, filtres de domaine, jeux d'enregistrements, dénomination de modèle CamelCase), scripts de migration (hooks pré/post/fin, migration de données), composants frontend OWL (hooks, services, cycle de vie), optimisation des performances (prévention N+1, opérations par lots, _read_group), rapports QWeb (PDF/HTML, formats papier, codes-barres, t-out), sécurité/ACL (règles d'enregistrement, autorisations de champ, groupes basés sur les privilèges, @api.private), tests (TransactionCase, HttpCase, moqueries, assertions du nombre de requêtes), transactions (points de sauvegarde, UniqueViolation, échecs de sérialisation), traductions (i18n, fichiers PO, champs traduisibles), vues XML (liste/formulaire/recherche, modèles de cartes Kanban, héritage XPath, modèles QWeb). À utiliser lors de l'écriture, de la révision ou du débogage de tout code Python ou XML Odoo 19, de la création ou de la modification de modules, de la résolution de problèmes de performances ou de la recherche de modèles et de bonnes pratiques d'API Odoo 19.
