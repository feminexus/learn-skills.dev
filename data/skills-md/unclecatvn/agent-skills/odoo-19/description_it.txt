Base di conoscenza per lo sviluppo di Odoo 19 con 18 guide specializzate che coprono azioni (ir.actions.*, lavori cron, azioni server), controller (routing HTTP, endpoint, tipi di autenticazione), file di dati (record XML/CSV, scorciatoie, noupdate), decoratori API (@api.depends, @api.constrains, @api.ondelete, @api.onchange, @api.model, @api.private), vincoli SQL (models.Constraint sostituisce _sql_constraints), Indici di database (models.Index), Sviluppo di moduli (manifest, procedure guidate, report), Tipi di campo (Char, Text, Monetary, campi relazionali), Configurazione manifest (__manifest__.py, dipendenze, pacchetti di risorse), Mixin (mail.thread, mail.activity.mixin, mail.alias.mixin, utm.mixin), Metodi del modello ORM (ricerca, CRUD, filtri di dominio, recordset, denominazione dei modelli CamelCase), script di migrazione (hook pre/post/fine, migrazione dei dati), componenti frontend OWL (hook, servizi, ciclo di vita), ottimizzazione delle prestazioni (prevenzione N+1, operazioni batch, _read_group), report QWeb (PDF/HTML, formati cartacei, codici a barre, t-out), sicurezza/ACL (regole record, permessi sui campi, gruppi basati sui privilegi, @api.private), test (TransactionCase, HttpCase, mocking, asserzioni di conteggio delle query), transazioni (punti di salvataggio, UniqueViolation, errori di serializzazione), traduzioni (i18n, file PO, campi traducibili), visualizzazioni XML (elenco/modulo/ricerca, modelli di carte kanban, ereditarietà xpath, modelli QWeb). Da utilizzare durante la scrittura, la revisione o il debug di qualsiasi codice Python o XML di Odoo 19, la creazione o la modifica di moduli, la risoluzione di problemi di prestazioni o la ricerca di modelli API e migliori pratiche di Odoo 19.
