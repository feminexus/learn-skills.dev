Lark/飞书实时事件监听/订阅/消费：通过“lark-cli事件消费<EventKey>”将事件以NDJSON流式传输（涵盖IM消息/反应/聊天更改、任务更新、VC会议开始/加入/结束、会议纪要生成、白板更新等）。用于 Lark 机器人、实时消息处理、长时间运行的订阅者、流式 Webhook/推送处理程序。支持 `--max-events` / `--timeout` 有界运行和 stderr 就绪标记合约 - 专为作为子进程运行的 AI 代理而设计。
