Lark/飛書即時事件監聽/訂閱/消費：透過「lark-cli事件消費<EventKey>」將事件以NDJSON串流（涵蓋IM訊息/反應/聊天變更、任務更新、VC會議開始/加入/結束、會議記錄產生、白板更新等）。用於 Lark 機器人、即時訊息處理、長時間運行的訂閱者、串流 Webhook/推播處理程序。支援 `--max-events` / `--timeout` 有界運行和 stderr 就緒標記合約 - 專為作為子進程運行的 AI 代理而設計。
