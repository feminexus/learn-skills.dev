قم بتنزيل مسودة SPEC على DUYỆT من خلال هذا الملف — ملف `DDMMYY-<tên>-PLAN.md` هو وكيل KHÔNG في السياق (CLI rẻ) chạy headless, không hại được) في الملف: ملف ملف chính xác، khối Interfaces (يستهلك/ينتج) في مهمة hàng xóm، ràng buộc bao trùm chép nguyên văn، ويتم تنفيذ 2-5 phút kiểu TDD có code thật + lệnh chạy + الإخراج من đợi. Gọi SAU `/propose` وSAU khi user duyệt ở cổng، TRƯỚC khi إرسال مهمة الوكيل. المشغل - "خطة viết"، "مقترح خطة thành Plan"، "خطة thi hành"، "وكيل وكيل مختصر"، "/ خطة". KHÔNG dùng để thiết kế hay để hỏi yêu cầu (đó là `/propose`)، KHÔNG dùng khi chưa có SPEC duyệt.
