Il s'agit d'un brouillon de SPEC qui vous permet d'obtenir ce fichier — le fichier `DDMMYY-<tên>-PLAN.md` pour l'agent KHONG avec le contexte (CLI est sans tête, không hỏi lại được) vẫn làm đúng : đường dẫn file chính xác, khối Interfaces (Consume/Produit) khai chữ ký cho task hàng xóm, ràng buộc bao trùm chép nguyên văn, et il y a 2 à 5 minutes avec le code TDD + + sortie + sortie. Gọi SAU `/propose` et SAU khi user duyệt ở cổng, TRƯỚC khi dispatch task cho agent. Déclencheur - "viết plan", "mở rộng proposition thành plan", "plan thi hành", "chuẩn bị brief cho agent", "/plan". KHÔNG dùng để thiết kế hay để hỏi yêu cầu (đó là `/proposer`), KHÔNG dùng khi chưa có SPEC duyệt.
