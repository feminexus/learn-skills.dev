Mở rộng một draft SPEC ĐÃ ĐƯỢC DUYỆT thành kế hoạch thi hành được — file `DDMMYY-<tên>-PLAN.md` mà một agent KHÔNG có context nào (CLI rẻ chạy headless, không hỏi lại được) vẫn làm đúng: đường dẫn file chính xác, khối Interfaces (Consume/Produce) khai chữ ký cho task hàng xóm, ràng buộc bao trùm chép nguyên văn, y từng bước 2-5 phút kiểu TDD có code thật + lệnh chạy + salida entre đợi. Gọi SAU `/propose` y SAU khi usuario duyệt ở cổng, TRƯỚC khi despacho tarea cho agente. Activador: "viết plan", "mở rộng propuesta thành plan", "plan thi hành", "chuẩn bị breve cho agente", "/plan". KHÔNG dùng để thiết kế hay để hỏi yêu cầu (đó là `/propose`), KHÔNG dùng khi chưa có SPEC duyệt.
