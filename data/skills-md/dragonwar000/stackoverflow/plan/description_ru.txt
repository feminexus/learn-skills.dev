Для проекта SPEC ĐÃ ĐƯỢC DUYỆT thành kế hoạch thi hành được — файл `DDMMYY-<tên>-PLAN.md` с агентом KHÔNG в контексте (CLI rẻ) chạy headless, không hỏi lại được) vẫn làm đúng: đường dẫn file chính xac, khối Интерфейсы (потребляет/производит) khai chữ ký cho Task, висит xóm, ràng buộc Если вы хотите получить больше информации, вы можете использовать 2-5 файлов TDD с кодом + длинный список + выходные данные. Если SAU `/propose` и пользователь SAU не работают, TRƯỚC выполняет задачу отправки агента. Триггер — «предложение по плану», «предложение по плану», «план по плану», «короткий план агента», «/plan». KHÔNG dùng để thiết kế hay để hỏi yêu cầu (đó là `/propose`), KHÔNG dùng khi chưa có SPEC duyệt.
