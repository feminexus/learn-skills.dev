ドラフト SPEC ĐÃ ĐƯỢC DUYỆT thành kế hoạch thi hành được — file `DDMMYY-<tên>-PLAN.md` mà một エージェント KHÔNG có context nao (CLI rẻ chạy headless、không hỏi lại được) vẫn làm đúng: đường dẫn file chính xác、khối インターフェイス (消費/生成) khai chữ ký cho タスク hàng xóm、ràng buộc Bao trùm chép nguyên văn、và từng bước 2-5 phút kiểu TDD có code thật + lệnh chạy + 出力 mong đợi。 SAU `/propose` は、SAU khi ユーザー duyệt ở cổng、TRƯỚC khi ディスパッチ タスク チョー エージェントです。トリガー - 「viết plan」、「mở rộng proposal thành plan」、「plan thi hành」、「chuẩn bị Brief cho Agent」、「/plan」。 KHÔNG dùng để thiết kế hay để hỏi yêu cầu (đó là `/propose`)、KHÔNG dùng khi chưa có SPEC duyệt。
