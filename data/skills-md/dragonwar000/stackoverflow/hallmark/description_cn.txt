SÀN design mặc định của overstack (anti-AI-slop) — mọi UI đứng trên nó：构建trang mới，审核，重新设计，提取设计từ URL/屏幕截图，và KHÓA HỆ THIẾT KẾ XUYÊN MÀN HÌNH qua design.md（chạy `hallmark redesign` cả app một lần → sinh design.md ở 项目根 → mọi màn hình sau bám cùng hệ token/type/motion, không mỗi màn một kiểu）。 Gọi khi 用户名称：'thiết kế'、'giao diện'、'làm UI/登陆页面/应用程序 mới'、'重新设计'、'审核设计'、'设计系统'、'đồng bộ giao diện giữa các màn hình'、'khóa hệ thiết kế'、'sao trang này trông AI 生成'、hoặc gọi đích danh 标志/dùng 动词构建·审计·重新设计·研究。 Việc chạm UI mà không chắc dùng gì → dùng Skill này（nó là sàn，các Skill attempts khác là pleasure bên trên）。
