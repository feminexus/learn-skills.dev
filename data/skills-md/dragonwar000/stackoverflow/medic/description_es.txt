Cómo usar el overstack del framework - gõ /medic (hoặc `medic` en CLI) para usar: luật còn cắn (arnés-doctor simulacro de incendio), validador không lệch bản (deriva), documentos khớp đĩa, compilación de código, eval không regresión, backstop git sống. Hub 1-tên — mô tả PHẠM VI thay vì nhớ subcomando (`medic` = all · `reglas médicas` · `evaluación de documentos médicos`). Dùng trước commit/PR, sau khi pull/đổi máy, khi nghi một rule không chặn nữa, hoặc vừa thêm habilidad/regla/generador (médico tự báo nếu quên hàng rào). Activador: /medic, "medic", "chạy medic", "hệ còn khoẻ không", "rà luật có cắn không", "cổng phòng thủ cuối", "health gate", "última línea de defensa".
