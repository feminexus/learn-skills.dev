Vòng xử lý SỰ CỐ première classe — bug khi có, temps d'exécution, hành vi sai, "approuver le processus", régression, production d'incidents. Điều phối triage → repro-first gate (chưa tái hiện được thì CHƯA được sửa) → khoanh vùng (code-graph/impact-check/bisect) → fix qua safe-change với bằng chứng đỏ→xanh → distiller kép (incident wiki + volant d'inertie + arbre à problèmes). KHÔNG dùng cho tính năng mới (đó là proposer/orca-workflow).
