Vòng xử lý SỰ CỐ first-class — dùng khi có bug, lỗi runtime, hành vi sai, "approve treo", regressione, produzione di incidenti. Điều phối triage → repro-first gate (chưa tái hiện được thì CHƯA được sửa) → khoanh vùng (code-graph/impact-check/bisect) → fix qua safe-change với bằng chứng đỏ→xanh → distill kép (incidente wiki + volano guasto + albero problema). KHÔNG dùng cho tính năng mới (đó là propose/orca-workflow).
