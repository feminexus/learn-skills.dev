Implemente una aplicación Docker-Compose a través de un Jenkins INBOUND AGENT ejecutándose en el servidor de destino (sin SSH del controlador), con refuerzo Docker-secrets "L3": los secretos de tiempo de ejecución se encuentran en tmpfs y nunca aparecen en `docker inspect` ni en `.env`. Úselo cuando el usuario quiera: "implementar a través del agente jenkins", "implementar sin ssh", "agente que vì ssh", "docker secrets L3", "secret khỏi docker inspect", "secret không nằm trên đĩa" o está conectando una canalización de Jenkins que implementa docker-compose en un host Linux. Codifica los errores aprendidos en una ejecución real 1:1 (Java 21, permisos secretos 0444, credencial de archivo 0400, el usuario del agente necesita Docker+sudo).
