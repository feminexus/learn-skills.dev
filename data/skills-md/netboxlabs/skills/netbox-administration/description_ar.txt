إدارة خادم NetBox: التكوين، والواجهات الخلفية للمصادقة (محلي/LDAP/SAML/OIDC)، وأذونات الكائنات، ورموز API المميزة، وضبط الأداء، والنسخ الاحتياطية، والترقيات، والتدبير المنزلي. يُستخدم عند إدارة مثيل NetBox بدلاً من استهلاك واجهة برمجة التطبيقات (API) الخاصة به.
