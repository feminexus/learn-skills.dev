Scaffold a new TheFocus.AI project from scratch — mise-managed tooling, pi config and extensions, required mise tasks, fnox + 1Password secrets, locked agent skills, AGENTS.md, and gitignore — so it runs with `mise dev` immediately. Scaffolding and configuration only, never application code. Use when starting a new project, bootstrapping a repo, or setting up tooling from zero. Triggers on: "new project", "start a project", "scaffold", "bootstrap", "set up a repo", "get this project started", "standard setup".
